name: foundation
runtime:
  name: nodejs
  options:
    packagemanager: yarn
main: src/index.ts
description: >-
  The organization's everyday configuration.
backend:
  url: gs://ms-root-foundation-state-b2099013
config:
  # Every resource names its project explicitly
  gcp:disableGlobalProjectWarning:
    value: true
