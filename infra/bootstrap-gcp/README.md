# GCP bootstrap stack

The root of trust for the GCP organization. It provisions only the minimal set of resources that the higher layers need to be managed safely by automation.

This stack is applied manually by an organization admin.
