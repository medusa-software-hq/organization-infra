# Bootstrap stack

The root of trust for the organization's infrastructure. It provisions only the minimal set of resources, in GCP and Pulumi Cloud, that the higher layers need to be managed safely by automation.

This stack is applied manually by an organization admin.
